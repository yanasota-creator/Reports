# レポート作成スタイルガイド

工学実験レポートをMarkdown（GitHub）で作成する際のルール。次回以降のレポートはこれに従って作成する。

## 1. 句読点

- 読点・句点は「、」「。」を使う（「，」「．」は使わない）

## 2. 太字（`**...**`）の使い方

- **OK**：見出し、表・図のキャプション（例：`**表5.1　○○**`）のみ
- **NG**：本文中で一部の語句・フレーズを強調するための太字（例：`〜が**重要である**．`のような使い方はしない）、箇条書きの項目名ラベルを太字にすること（例：`**直交ゲージ**：〜`のような使い方はしない）

## 3. 数式の書き方

- **文中に埋め込む小さい数式**：前後にスペースを入れず、地の文に詰めて書く
  - 例：`縦弾性係数は$E_{実験}=221{,}272\ \text{MPa}$であった。`
- **1行（段落）を使う大きいブロック数式**（`$$ ... $$`）：前後に空行を入れて独立した段落にする（スペースの詰め込みは不要）

### 閉じ`$`の直後に半角英数字を置かない（重要）

閉じ`$`の直後が半角英数字だと、その`$`が閉じ記号として認識されず、**次の`$`と誤って対応してしまう**。その結果、間に挟まれた日本語の地の文がまるごと数式として組まれ、「一部だけ急に文字が大きくなる・太字に見える」という崩れ方をする。

**単位は数式の外に出さず、`\text{}`で数式の中に入れる。**

```
NG： $\lambda=635$nm        $h=6.626\times10^{-34}$J·s        $W=0.816$eV
OK： $\lambda=635\ \text{nm}$   $h=6.626\times10^{-34}\ \text{J·s}$   $W=0.816\ \text{eV}$
```

閉じ`$`の直後が日本語（`を` `の` `、` `）`など）であれば問題ない。レポートを書いた後は次のコマンドで残っていないか確認する。

```sh
python3 - <<'PY'
import re
for i,l in enumerate(open('report.md',encoding='utf-8'),1):
    idx=[m.start() for m in re.finditer(r'\$', l)]
    for a,b in zip(idx[0::2], idx[1::2]):
        nxt = l[b+1] if b+1 < len(l) else ''
        if re.match(r'[A-Za-z0-9]', nxt):
            print(f'{i}: {l[a:b+2]} ← 閉じ$の直後が "{nxt}"')
PY
```

## 4. 表・図のキャプション

- 表には**表X.X　タイトル**、図には**図X　タイトル**のキャプションを付ける（本文中で「図1」「表5.2」のように参照している番号と一致させる）
- 表のキャプションは表の**上**、図のキャプションは図（画像）の**下**に置く
- 図（画像）は、対応する本文（説明文）の**直下**に配置する
- 表の列の配置：**数値の列は右寄せ**（`---:`）、**文字列（項目名等）の列は左寄せ**（`:---`）、**見出し的に短い記号だけの列は中央寄せ**（`:---:`）でも可。数値列は右寄せに統一する

## 5. 参考文献

- `[1]`、`[2]`...の各項目は、**間に空行を1つ入れて**改行する（空行が無いとGitHub上で1行に繋がって表示されてしまう）
- 書式：`[番号] 出典名．「タイトル」．URL　参照日:YYYY/M/D`

```
[1] 出典名．「タイトル」．https://example.com　参照日:2026/6/28

[2] 出典名．「タイトル」．https://example.com　参照日:2026/6/28
```

## 6. ファイル名・フォルダ構成

- ファイル名は**英数字とアンダースコアのみ**（日本語・スペースは使わない）
  - 日本語ファイル名はURLが極端に長くなり読み込みエラーの原因になる
  - スペースはMarkdown PDFが画像を読み込めなくなる原因になる（`Stress-strain diagram.png` → NG、`Stress-strain_diagram.png` → OK）
- 構成：

```
report.md / report_xxx.md   ← リポジトリ直下
data/                        ← 測定データ（csv等）
images/                      ← グラフ・図（png推奨、pdfはMarkdown上で表示されないため不可）
```

## 7. レポートの基本構成

```
1. 目的
2. 使用機器
3. 実験原理
4. 実験手順
5. 実験結果（表・図を含む）
6. 考察
（必要な場合のみ）IV. 追加調査課題
参考文献
```

## 8. 考察を書く上での姿勢

- データに異常値がある場合、消さずにそのまま記載し、考察で原因を推測する（操作ミス・読み取りミスなど、具体的な状況がわかっていればそれを書く）
- 文献値との比較を行う場合は、誤差(%)を計算して明記し、出典を参考文献に記載する

## 9. グラフ・図の描き方

- データ点は大きく、はっきりと描く
- データ点同士を単純な折れ線で結ばない。最小二乗法などによる回帰直線・回帰曲線のように、根拠のある線を引く
- 軸には必ず物理量と単位を明記する（例：`コイル電流 I [A]`）
- 目盛りの数値は読み取りやすい間隔にし、目盛り線（グリッド線）を入れる
- グラフには必ずタイトルをつける
- 白黒印刷を想定し、複数系列を1つのグラフに重ねる場合は、色だけでなく線種（実線・破線・点線・一点鎖線）とマーカー形状（○・□・△・×など）を組み合わせて区別できるようにする
  - **このルールが必要なのは、1つのグラフに系列が2本以上あるときだけ**。系列が1本のグラフ（例：測定値と回帰直線だけのグラフ）には、線種・マーカー形状の描き分けは不要
  - 逆に、系列が2本以上あるのに色だけで区別しているグラフは、レポートの確認時に指摘して描き直す

## 10. 参考文献の本文中引用

- 本文中で参考文献を引用する箇所は、上付き文字で書く
- Markdownでは`<sup>[1]</sup>`のようにHTMLタグを使う

## 11. PDF出力の体裁（字下げ・明朝体・ページ番号）

PDFはVSCode拡張「Markdown PDF」（yzane.markdown-pdf）で出力する。体裁は次の2か所で共通管理しており、**レポートごとに書く必要はない**（新しい実験のレポートにもそのまま適用される）。

### 11.1 見た目のルール（ルートの`pdf_style.css`）

- 本文は**明朝体**（`Yu Mincho`→`MS PMincho`等の順にフォールバック）
- 段落の先頭は**全角1文字分の字下げ**（`p { text-indent: 1em; }`）
- 字下げしないもの：見出し、箇条書き、表の中身、画像、表・図のキャプション（`**...**`だけの段落）、ブロック数式
- 数式（KaTeX）中の日本語（`h_{実験}`など）にも明朝体を指定する
  - 指定しないと日本語グリフだけ中国語フォントにフォールバックし、その部分が急に太字になったように見える
- 数式の文字サイズを本文に合わせる（インライン`1.05em`／ブロック`1.15em`）
  - KaTeXの既定は`1.21em`（欧文本文に合わせた設計）で、和文本文と並べると数式だけ大きく＝太く見える
- 見出しの直後や、図・表・数式ブロックの途中で改ページされないようにする
- あわせて、PDFの余白（`markdown-pdf.margin.*`）を既定より広くとる
  - 既定の下余白1cmではフッタ（ページ番号）と本文がぶつかり、改ページ位置で本文の行が切れることがある

### 11.2 VSCode側の設定（`.vscode/settings.json`）

リポジトリ直下の`.vscode/settings.json`に以下を置いてある。VSCodeでReportsフォルダをワークスペースとして開いていれば自動で効く。

```json
{
  "markdown-pdf.styles": ["pdf_style.css"],
  "markdown-pdf.stylesRelativePathFile": false,
  "markdown-pdf.displayHeaderFooter": true,
  "markdown-pdf.footerTemplate": "<div style=\"font-size:9px; margin:0 auto;\"><span class='pageNumber'></span></div>",
  "markdown-pdf.margin.top": "2cm",
  "markdown-pdf.margin.bottom": "1.8cm",
  "markdown-pdf.margin.left": "1.5cm",
  "markdown-pdf.margin.right": "1.5cm"
}
```

- `markdown-pdf.styles`：`pdf_style.css`を読み込ませる。**これが無いと字下げ・明朝体が反映されない**
  - `stylesRelativePathFile: false`のとき、相対パスはワークスペースルート（Reportsフォルダ）からの解釈になる
- `markdown-pdf.footerTemplate`：**ページ番号を「2 / 7」ではなく「2」だけにする**
  - 既定値は`<span class='pageNumber'></span> / <span class='totalPages'></span>`なので、`/ totalPages`の部分を消している
- Reportsフォルダをワークスペースとして開かずに使う場合は、同じ内容をユーザー設定（`Ctrl+Shift+P`→「基本設定: ユーザー設定を開く (JSON)」）に書き、`markdown-pdf.styles`はフルパスで指定する

### 11.3 出力手順

Markdownファイルを開いた状態で`Ctrl+Shift+P`→`Markdown PDF: Export (pdf)`。設定を変更した場合は、変更後に出力し直さないと反映されない。
